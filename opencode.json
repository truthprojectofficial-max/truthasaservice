{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "build",
  "instructions": ["AGENTS.md"],
  "agent": {
    "build": {
      "description": "OGIR build agent. Runs the SESSION START RITUAL (verify chain, read handover, read INDEX, run tests, seal SIGN_ON) before any work."
    }
  }
}