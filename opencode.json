{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "build",
  "instructions": ["AGENTS.md"],
  "skills": {
    "paths": [".opencode/skills"]
  },
  "mcp": {
    "memory": {
      "type": "local",
      "command": ["cmd", "/c", "npx", "-y", "@modelcontextprotocol/server-memory"],
      "enabled": true,
      "timeout": 10000
    },
    "git": {
      "type": "local",
      "command": ["uvx", "mcp-server-git", "--repository", "C:\\Users\\justo\\OneDrive\\Documents\\My Project\\OrderGetItRight"],
      "enabled": true,
      "timeout": 10000
    },
    "sequential-thinking": {
      "type": "local",
      "command": ["cmd", "/c", "npx", "-y", "@modelcontextprotocol/server-sequentialthinking"],
      "enabled": true,
      "timeout": 10000
    }
  },
  "agent": {
    "build": {
      "description": "OGIR build agent. Runs the SESSION START RITUAL (verify chain, read handover, read INDEX, run tests, seal SIGN_ON) before any work. Loaded with skills: second-opinion (delegation to stronger models), credential-hygiene (catch secrets before commit), workaround-discipline (seal shortcuts), handover-verification (verify chain witnesses), priority-check (follow the priority list). MCP tools: memory (persistent knowledge graph), git (repo history), sequential-thinking (structured reasoning).",
      "model": "ollama/glm-5.2:cloud"
    }
  }
}